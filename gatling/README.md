clone the gatling repository to a new load test project:

```
git clone git@github.com:gatling/gatling-maven-plugin-demo.git ${NAME_OF_PROJECT}-load-test
```

run a project with

```
mvn gatling:test
```